server:
  port: 8081
  error:
    path: /error

spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  application:
    name: stableford-scoring-service
  security:
    oauth2:
      resourceserver:
        jwt:
          # Note the trailing slash is important!
          issuer-uri: https://connective-dev.au.auth0.com/
#  datasource:
#    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
#    url: jdbc:sqlserver://10.70.0.8;databaseName=notes;trustServerCertificate=true
#    username: dev_user
#    password: 4nKi6Yc94kM9oU6ETVEqaO3
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServer2012Dialect
    open-in-view: 'false'
    hibernate:
      ddl-auto: none
    show-sql: 'true'
  jackson:
    deserialization:
      fail-on-unknown-properties: 'false'